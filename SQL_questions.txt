1. Write a SQL query that returns the addresses of homes worth more than 1 million dollars

2. Write a SQL query that returns the mean number of bedrooms and bathrooms for each property type (mean bed and bath for House, Apartment, Unit)

3. Write a SQL query that returns the third-highest price for each property type